package org.mattshoe.shoebox.kernl

import kotlin.time.Duration

/**
 * Represents an interval that never elapses.
 */
val NEVER = Duration.INFINITE

/**
 * Represents a duration of time that never ends.
 */
val FOREVER = Duration.INFINITE